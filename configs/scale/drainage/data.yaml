# Scale-Up test data for the drainage campaign — shared by every configs/scale/drainage/*.yaml cell.
root: data/case              # the PoreML dataset: `poreml download` puts it here
campaign: drainage
split: splits/scale/drainage.yaml    # frozen, eval-only: every finished 256-class run of the campaign,
                                     # generated and uCT alike
                                     # (16 runs: blob 3, poly 3, sphere 2, bentheimer 3, buffberea 3, castlegate 2 @256^3);
                                     # drawn by case/scale/run.py split
require_finished: true
